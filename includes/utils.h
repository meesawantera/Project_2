#ifndef UTILY_BOI_H_
#define UTILY_BOI_H_

#include "constants.h"

    void sortData(const SORT_ORDER &mySortOrder,vector<process> &myProcesses)
    process getNext(vector<process> &myProcesses)
    int getSize(vector<process> &myProcesses
    int handleMissingData(vector<process> &myProcesses)4

#endif